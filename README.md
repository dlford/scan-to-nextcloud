# Moved to Codeberg

This project has migrated to [Codeberg](https://codeberg.org/dlford/scan-to-nextcloud).

Please update your docker image to point to `codeberg.org` instead of `ghcr.io` starting with version 1.0.2, for example: `codeberg.org/dlford/scan-to-nextcloud:latest`.
